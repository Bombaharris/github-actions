{
  "name": "github-actions",
  "version": "2.0.1",
  "description": "Demo actions",
  "main": "index.js",
  "scripts": {
    "test:ci": "exit 0",
    "build:single-spa:github-actions": "exit 0",
    "changelog": "conventional-changelog -i CHANGELOG.md -o CHANGELOG.md  -s -k package.json -t v"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serve": "^14.2.3"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^5.0.0"
  }
}
